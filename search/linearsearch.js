const linerSearch = (n, arr) => {
  let arrLength = arr.length;
  if (arrLength === 0) return -1;
  for (number of arr) {
    if (number === n) return arr.indexOf(number);
  }
  return -1;
};

console.log(linerSearch(10, [-5, 2, 10, 4, 6]));
console.log(linerSearch(20, [-5, 2, 10, 4, 6]));
